import { mount } from '@vue/test-utils'
import Ok{{pascalCase name}} from './'

describe('Ok{{pascalCase name}}', () => {
  it('renders main components', () => {
    const wrapper = mount(Ok{{pascalCase name}})
    expect(wrapper).toBeInstanceOf(Object)
  })
})
